# Optional: call backend directly from the browser (default uses /api proxy via next.config rewrites)
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: override proxy target (used by next.config rewrites)
# BACKEND_URL=http://localhost:8000